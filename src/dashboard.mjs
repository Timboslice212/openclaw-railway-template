const escapeHtml = (value) =>
  String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");

export function dashboardHtml() {
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="dark"><title>Set up OpenClaw on Railway</title>
<style>
:root{--bg:#080b12;--panel:#121827;--panel2:#0d121d;--line:#263149;--muted:#9aa8bd;--text:#f6f8fc;--green:#42d692;--yellow:#ffc966;--red:#ff7380;--accent:#7c8cff;--accent2:#9d7cff}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 12% -10%,#202b4f 0,transparent 35%),radial-gradient(circle at 90% 10%,#231d43 0,transparent 28%),var(--bg);color:var(--text);font:15px/1.5 Inter,ui-sans-serif,system-ui,-apple-system,sans-serif}main{width:min(1040px,calc(100% - 28px));margin:42px auto 70px}.top{display:flex;justify-content:space-between;gap:24px;align-items:flex-start;margin-bottom:24px}.eyebrow{color:#aab4ff;font-weight:800;letter-spacing:.13em;text-transform:uppercase;font-size:12px}h1{font-size:clamp(32px,5vw,52px);line-height:1.02;margin:7px 0 12px;letter-spacing:-.035em}.lead{color:var(--muted);max-width:690px;margin:0;font-size:16px}.badge{border:1px solid var(--line);background:#0d1220;border-radius:999px;padding:8px 13px;white-space:nowrap}.dot{width:9px;height:9px;border-radius:50%;display:inline-block;margin-right:8px;background:var(--yellow)}.progress{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin:24px 0}.step{border:1px solid var(--line);background:var(--panel2);border-radius:12px;padding:11px 13px;color:var(--muted);font-weight:700}.step.active{border-color:var(--accent);color:var(--text);box-shadow:inset 0 0 0 1px var(--accent)}.step.done{color:var(--green);border-color:#285e4a}.grid{display:grid;grid-template-columns:.85fr 1.4fr;gap:16px}.card{background:linear-gradient(180deg,rgba(21,29,46,.98),rgba(13,18,29,.98));border:1px solid var(--line);border-radius:18px;padding:22px;box-shadow:0 18px 55px rgba(0,0,0,.18)}.wide{grid-column:1/-1}h2{font-size:18px;margin:0 0 6px}.muted{color:var(--muted)}.check{display:flex;gap:10px;align-items:flex-start;padding:12px 0;border-bottom:1px solid #20293b}.check:last-child{border:0}.icon{width:22px;height:22px;display:grid;place-items:center;border-radius:50%;font-size:12px;font-weight:900;background:#263149;color:var(--muted)}.icon.ok{background:#153d31;color:var(--green)}.icon.bad{background:#48242b;color:var(--red)}label{display:block;font-weight:750;margin:16px 0 7px}.hint{display:block;color:var(--muted);font-size:13px;font-weight:400;margin-top:4px}input,select{width:100%;background:#080c14;color:var(--text);border:1px solid var(--line);border-radius:11px;padding:12px 13px;font:inherit}input:focus,select:focus{outline:2px solid #5968c8;border-color:transparent}.row{display:flex;gap:10px;align-items:center;flex-wrap:wrap}button,a.button{appearance:none;border:0;border-radius:11px;padding:12px 15px;font:inherit;font-weight:800;cursor:pointer;text-decoration:none;background:linear-gradient(135deg,var(--accent),var(--accent2));color:white}button.secondary{background:#222b3e}button.danger{background:#662f39}button:disabled{opacity:.55;cursor:wait}.hidden{display:none}.notice{margin-top:16px;padding:12px 14px;border:1px solid var(--line);border-radius:11px;background:#0b101a;color:var(--muted)}.notice.error{color:#ffd5d9;border-color:#66333b;background:#241318}.notice.success{color:#c9f8df;border-color:#285e4a;background:#10251e}pre{background:#080c14;border:1px solid #222c3e;border-radius:12px;padding:14px;min-height:80px;max-height:360px;overflow:auto;white-space:pre-wrap;color:#c9d5e8}code{color:#cbd5ff}.launch{font-size:16px;padding:14px 20px}@media(max-width:760px){main{margin-top:26px}.top{display:block}.badge{display:inline-block;margin-top:18px}.grid{grid-template-columns:1fr}.wide{grid-column:auto}.progress{grid-template-columns:1fr 1fr}.step{font-size:13px}}
</style></head><body><main>
<section class="top"><div><div class="eyebrow">Latest stable · Railway edition</div><h1>OpenClaw, ready in one guided setup.</h1><p class="lead">Configure your AI provider, choose a model, optionally connect a channel, and enter the official dashboard with secure browser authorization—without a terminal or copied Gateway token.</p></div><div class="badge"><span class="dot" id="statusDot"></span><span id="statusText">Checking deployment…</span></div></section>
<div class="progress"><div class="step active" data-step="preflight">1 · Preflight</div><div class="step" data-step="provider">2 · AI provider</div><div class="step" data-step="channel">3 · Channel</div><div class="step" data-step="security">4 · Security</div><div class="step" data-step="complete">5 · Launch</div></div>
<section class="grid">
<article class="card"><h2>Deployment checks</h2><p class="muted">Credentials are saved only after the deployment is durable and publicly reachable.</p><div id="checks"><div class="check"><span class="icon">…</span><div>Loading environment</div></div></div></article>
<article class="card"><h2>First-run configuration</h2><p class="muted">Credentials travel over HTTPS and stay in OpenClaw's private state on the attached Railway volume.</p>
<form id="setupForm" method="post" action="/setup"><label for="provider">AI provider</label><select id="provider" name="provider" required><option value="openai">OpenAI</option><option value="anthropic">Anthropic</option><option value="google">Google Gemini</option><option value="openrouter">OpenRouter</option><option value="xai">xAI (Grok)</option><option value="none">Configure provider later in OpenClaw</option></select>
<label for="apiKey">Provider API key<span class="hint">The key is never returned by this app or written to deployment logs.</span></label><input id="apiKey" name="apiKey" type="password" autocomplete="off" required minlength="8">
<label for="model">Model override <span class="muted">(optional)</span><span class="hint" id="modelHint"></span></label><input id="model" name="model" autocomplete="off">
<label for="channel">Messaging channel <span class="muted">(optional)</span></label><select id="channel" name="channel"><option value="none">Skip for now</option><option value="telegram">Telegram</option><option value="discord">Discord</option></select>
<div class="hidden" id="channelFields"><label for="channelToken"><span id="channelName">Channel</span> bot token<span class="hint">You can add or change channels later inside OpenClaw.</span></label><input id="channelToken" name="channelToken" type="password" autocomplete="off"></div>
<div class="notice">Setup validates OpenClaw configuration, provider route, optional channel, and Gateway readiness before Launch.</div><div class="row" style="margin-top:18px"><button id="install" type="submit">Configure & validate OpenClaw</button><a href="/logout" class="muted">Sign out</a></div></form>
<div id="completePanel" class="hidden"><div class="notice success"><strong>OpenClaw is ready.</strong><br>The Gateway passed readiness checks and your configuration is persisted. Future provider, model, and channel changes are available inside OpenClaw Settings.</div><div class="row" style="margin-top:18px"><button class="launch" id="launch">Launch secure dashboard</button></div></div><div id="result" class="notice hidden"></div></article>
<article class="card wide"><details><summary><strong>Recovery diagnostics</strong> <span class="muted">— only needed when a check fails</span></summary><div class="row" style="margin-top:16px"><button type="button" class="secondary action" data-action="doctor">Run doctor</button><button type="button" class="secondary action" data-action="status">Gateway status</button><button type="button" class="danger action" data-action="restart">Restart Gateway</button></div><pre id="output">Diagnostics have not been run.</pre></details></article>
</section></main><script>
const $=(s)=>document.querySelector(s);let currentStatus=null;
async function api(path,options){const response=await fetch(path,options);const data=await response.json().catch(()=>({ok:false,error:'Invalid server response'}));if(!response.ok)throw new Error(data.error||('HTTP '+response.status));return data}function post(path,body){return api(path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)})}
function checkRow(ok,title,detail){const row=document.createElement('div');row.className='check';const icon=document.createElement('span');icon.className='icon '+(ok?'ok':'bad');icon.textContent=ok?'✓':'!';const copy=document.createElement('div');const strong=document.createElement('strong');strong.textContent=title;const small=document.createElement('div');small.className='muted';small.textContent=detail;copy.append(strong,small);row.append(icon,copy);return row}
function setProgress(step){const aliases={model:'provider',validation:'security',gateway:'security'};const selected=aliases[step]||step;const order=['preflight','provider','channel','security','complete'];const index=Math.max(0,order.indexOf(selected));for(const el of document.querySelectorAll('.step')){const i=order.indexOf(el.dataset.step);el.classList.toggle('done',i<index);el.classList.toggle('active',i===index)}}
function showResult(message,error=false){const box=$('#result');box.textContent=message;box.className='notice '+(error?'error':'success')}
function render(status){currentStatus=status;const checks=$('#checks');checks.replaceChildren();checks.append(checkRow(String(status.version||'').includes('2026.9.2'),'OpenClaw 2026.9.2',status.version||'Version unavailable'));checks.append(checkRow(status.storage.persistent&&status.storage.writable,'Persistent storage',status.storage.persistent?'Railway volume mounted at /data':'Missing Railway volume at /data'));checks.append(checkRow(Boolean(status.publicOrigin),'Public HTTPS domain',status.publicOrigin||'No Railway public domain detected'));const ready=status.setup.status==='complete';$('#statusText').textContent=ready?'Ready to launch':status.setup.status==='running'?'Setup running…':'Setup required';$('#statusDot').style.background=ready?'var(--green)':status.setup.status==='failed'?'var(--red)':'var(--yellow)';setProgress(ready?'complete':status.setup.step||'preflight');$('#setupForm').classList.toggle('hidden',ready);$('#completePanel').classList.toggle('hidden',!ready);if(status.setup.status==='failed'&&status.setup.error)showResult(status.setup.error,true)}
async function refresh(){try{render(await api('/setup/api/status'))}catch(error){$('#statusText').textContent='Control center error';$('#statusDot').style.background='var(--red)';showResult(error.message,true)}}
function syncProvider(){const provider=$('#provider').value;const deferred=provider==='none';const apiKey=$('#apiKey');const model=$('#model');const channel=$('#channel');apiKey.required=!deferred;apiKey.disabled=deferred;model.disabled=deferred;channel.disabled=deferred;if(deferred){apiKey.value='';model.value='';channel.value='none';model.placeholder='Configure later in OpenClaw Settings';$('#modelHint').textContent='Provider and model setup will be completed from the OpenClaw dashboard.';$('#install').textContent='Launch OpenClaw without provider'}else{model.placeholder=provider+'/model-id';$('#modelHint').textContent='Leave blank for the provider default, or enter '+provider+'/model-id.';$('#install').textContent='Configure & validate OpenClaw'}syncChannel()}function syncChannel(){const value=$('#channel').value;$('#channelFields').classList.toggle('hidden',value==='none');$('#channelToken').required=value!=='none'&&!$('#channel').disabled;$('#channelName').textContent=value==='none'?'Channel':value[0].toUpperCase()+value.slice(1)}
$('#provider').onchange=syncProvider;$('#channel').onchange=syncChannel;syncProvider();syncChannel();
$('#setupForm').onsubmit=async(event)=>{event.preventDefault();const button=$('#install');const deferred=$('#provider').value==='none';button.disabled=true;button.textContent=deferred?'Starting OpenClaw…':'Configuring OpenClaw…';showResult(deferred?'Starting the Gateway. Add your provider later in OpenClaw Settings.':'Running official provider setup and validation. This can take up to two minutes.');setProgress(deferred?'gateway':'provider');try{const values=Object.fromEntries(new FormData(event.target));await post('/setup/api/install',values);event.target.reset();syncProvider();syncChannel();showResult(deferred?'OpenClaw is ready. Configure your provider from the dashboard.':'Configuration validated successfully.');await refresh()}catch(error){showResult(error.message,true);await refresh()}finally{button.disabled=false;syncProvider()}};
$('#launch').onclick=async()=>{const button=$('#launch');button.disabled=true;button.textContent='Authorizing browser…';try{const handoff=await post('/setup/api/handoff',{});window.location.assign(handoff.url)}catch(error){showResult(error.message,true);button.disabled=false;button.textContent='Launch secure dashboard'}};
function formatDiagnostic(data){if(data.kind==='container-status'){const s=data.status;return ['Railway container status',s.gatewayReady?'✓ Gateway readiness probe passed':'✗ Gateway is not ready',s.processRunning?'✓ Gateway process is running'+(s.pid?' (PID '+s.pid+')':''):'✗ Gateway process is not running','Endpoint: '+s.endpoint,'Exposure: '+s.exposure,'Public origin: '+(s.publicOrigin||'unavailable'),'Persistent storage: '+(s.persistentState.persistent&&s.persistentState.writable?'ready at /data':'not ready'),'Version: '+(s.version||'checking'),s.lastError?'Last error: '+s.lastError:'No recorded Gateway error.'].join('\\n')}if(data.kind==='doctor'){const r=data.report;const findings=Array.isArray(r.findings)?r.findings:[];const lines=[r.ok?'OpenClaw Doctor: healthy':'OpenClaw Doctor: review recommended','Checks run: '+(r.checksRun??'n/a')+' · skipped: '+(r.checksSkipped??'n/a')];if(!findings.length)lines.push('✓ No findings.');for(const f of findings)lines.push('\\n['+String(f.severity||'info').toUpperCase()+'] '+String(f.message||f.checkId||'Finding')+(f.fixHint?'\\nAction: '+f.fixHint:''));return lines.join('\\n')}return String(data.output||JSON.stringify(data,null,2))}
for(const button of document.querySelectorAll('.action'))button.onclick=async()=>{button.disabled=true;$('#output').textContent='Running…';try{const data=await post('/setup/api/action',{action:button.dataset.action});$('#output').textContent=formatDiagnostic(data)}catch(error){$('#output').textContent=error.message}finally{button.disabled=false;refresh()}};refresh();setInterval(()=>{if(currentStatus&&currentStatus.setup.status==='running')refresh()},3000);
</script></body></html>`;
}

export function loginHtml(message = "") {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="dark"><title>OpenClaw Setup Login</title><style>:root{--bg:#080b12;--line:#263044;--muted:#9eabc0;--text:#f4f7fb;--red:#ff6b78;--accent:#7c8cff}*{box-sizing:border-box}body{margin:0;min-height:100vh;display:grid;place-items:center;background:radial-gradient(circle at 20% 0,#18223c 0,transparent 40%),var(--bg);color:var(--text);font:15px/1.5 Inter,ui-sans-serif,system-ui,sans-serif;padding:20px}main{width:min(440px,100%);background:linear-gradient(180deg,#171f31,#0e131f);border:1px solid var(--line);border-radius:20px;padding:28px;box-shadow:0 24px 80px rgba(0,0,0,.35)}h1{margin:0 0 8px;font-size:28px}p{color:var(--muted)}label{display:block;margin:20px 0 7px;font-weight:700}input{width:100%;background:#080c14;color:var(--text);border:1px solid var(--line);border-radius:11px;padding:12px 14px;font:inherit}button{width:100%;margin-top:16px;border:0;border-radius:11px;padding:12px 14px;background:var(--accent);color:white;font:inherit;font-weight:800;cursor:pointer}.error{color:var(--red)}</style></head><body><main><div style="color:#aab4ff;font-weight:800;font-size:12px;letter-spacing:.12em;text-transform:uppercase">Railway first run</div><h1>Secure OpenClaw setup</h1><p>Enter the setup password you chose in the Railway deployment form. No Variables-tab edit or redeploy is required.</p>${message ? `<p class="error">${escapeHtml(message)}</p>` : ""}<form method="post" action="/login"><label for="password">Setup password</label><input id="password" name="password" type="password" autocomplete="current-password" required autofocus><button type="submit">Continue securely</button></form></main></body></html>`;
}

export function errorHtml(title, detail) {
  return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${escapeHtml(title)}</title><style>body{font-family:system-ui;background:#0b0e15;color:#f4f7fb;margin:40px auto;max-width:760px;padding:0 20px}main{background:#151b28;border:1px solid #2a3448;border-radius:18px;padding:24px}p{color:#b1bdd0;white-space:pre-wrap}code{color:#cbd5ff}</style></head><body><main><h1>${escapeHtml(title)}</h1><p>${escapeHtml(detail)}</p></main></body></html>`;
}