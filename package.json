{
  "name": "openclaw-railway-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node --check src/server.mjs && node --check src/dashboard.mjs && node --check src/healthcheck.mjs",
    "test": "node --test"
  }
}
